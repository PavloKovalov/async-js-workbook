{
  "name": "promises",
  "version": "0.2.0",
  "description": "",
  "scripts": {
    "ajax": "cd ajax && budo index.js --live -- -t babelify",
    "callbacks": "cd callbacks && budo index.js:bundle.js --live -- -t babelify",
    "promises": "cd promises && budo index.js:bundle.js --live -- -t babelify"
  },
  "author": "Pavel Kovalyov <pavel.kovalyov@gmail.com>",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.3.0",
    "budo": "^8.2.2"
  }
}
